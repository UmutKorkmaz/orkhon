arch_path: configs/model/smoke_6m.yaml
init_from: runs/sft_smoke
ref_from: runs/sft_smoke
tokenizer: {dir: artifacts/tokenizer/smoke}
data: {rlvr_path: data/rlvr/arithmetic.jsonl}
optim: {lr: 1.0e-5, warmup_steps: 0, grad_clip: 1.0}
train: {device: cpu, dtype: float32, seed: 1337, batch_size: 2, max_steps: 6, seq_len: 64, log_interval: 1, eval_interval: 100, ckpt_interval: 6, out_dir: runs/_grpo_smoke}
group_size: 4
beta: 0.02
max_new_tokens: 16
temperature: 0.9
top_p: 0.95
reward_kind: math
